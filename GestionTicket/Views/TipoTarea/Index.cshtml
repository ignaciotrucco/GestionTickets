@{
  ViewData["Title"] = "Tipos Tareas";
}

<div class="text-center">
    <h2 class="display-4">Tipos de Tareas</h2>
</div>

<div class="row mt-4">
    <div class="col-md-3">
        <div class="card">
            <div class="card-header">
                <h5>Agregar tipo de tarea</h5>
            </div>
            <div class="card-body">
                <form>
                  <input type="hidden" id="tipoID" value="0"/>
                  <input type="text" class="form-control" id="nombreTipo" placeholder="Nombre:" required>
                  <span class="spanError animate__animated animate__fadeIn" style="display: none;" id="nombreTipoError"><i class="fa-solid fa-circle-exclamation errorIcon"></i> Ingresar nombre</span>
                </form>
                <div class="d-flex justify-content-between mt-3">
                    <button type="button" class="btn btn-primary" onclick="crearTipoTarea()">Guardar</button>
                    <button type="button" class="btn btn-primary" onclick="listaTipoTareas()">Recargar</button>
                </div>
            </div>
        </div>
    </div>
    <div class="col-md-9">
        <div class="card">
            <div class="card-header">
                <h5>Listado</h5>
            </div>
            <div class="card-body">
                <table class="table">
                    <thead>
                        <tr>
                        <th class="thTabla">Tipo de Tarea</th>
                        <th style="width: 70px"></th>
                        <th style="width: 70px"></th>
                        </tr>
                    </thead>
                    <tbody id="tbody-listaTareas"></tbody>
                </table>
            </div>
        </div>
    </div>
</div>


<!-- Modal -->
<div class="modal fade" id="modalTipoTarea" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">
      <div class="modal-header">
        <h1 class="modal-title fs-5" id="modalTitulo"></h1>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>
      <div class="modal-body">
        <form>
          <div class="row">
            <input type="hidden" id="tipoTareaEditarID" value="0"/>
            <div class="mb-3 col-12">
              <label for="nombreTipoEditar" class="form-label">Nombre tipo de tarea:</label>
              <input type="text" class="form-control" id="nombreTipoEditar" placeholder="Nombre:" required>
              <span class="spanError animate__animated animate__fadeIn" style="display: none;" id="nombreTipoErrorEdit"><i class="fa-solid fa-circle-exclamation errorIcon"></i> Ingresar nombre</span>
            </div>
          </div>
        </form>
      </div>
      <div class="modal-footer">
        <button type="button" onclick="limpiarCampos()" class="btn btn-danger" data-bs-dismiss="modal"><i class="fas fa-solid fa-ban"></i> Cancelar</button>
        <button type="button" onclick="editarTarea()" class="btn btn-success"><i class="fas fa-solid fa-plus"></i> Guardar</button>
      </div>
    </div>
  </div>
</div>

@section Scripts{
  <script src="~/js/tipoTarea.js"></script>
}